<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/linearLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <EditText
        android:id="@+id/eventDetail_detailListItem_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:minWidth="64dp"
        android:textAppearance="?attr/textAppearanceListItem"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:autofillHints="@string/eventDetail_detailList_title"
        android:inputType="text"
        android:hint="@string/eventDetail_detailList_title"/>

    <EditText
        android:id="@+id/eventDetail_detailListItem_content"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:textAppearance="?attr/textAppearanceListItem"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/eventDetail_detailListItem_delButton"
        app:layout_constraintStart_toEndOf="@id/eventDetail_detailListItem_title"
        app:layout_constraintTop_toTopOf="parent"
        android:autofillHints="@string/eventDetail_detailList_content"
        android:inputType="text"
        android:hint="@string/eventDetail_detailList_content"/>

    <ImageButton
        android:id="@+id/eventDetail_detailListItem_delButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:padding="12dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/delete_24"
        android:contentDescription="@string/eventDetail_deleteDetail" />


</androidx.constraintlayout.widget.ConstraintLayout>